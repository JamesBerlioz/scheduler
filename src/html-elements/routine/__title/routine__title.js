import { routine } from "../routine";

export const routine__title = document.createElement("h3");

routine__title.className = "routine__title";
routine__title.innerHTML = "Today:";

routine.append(routine__title);
